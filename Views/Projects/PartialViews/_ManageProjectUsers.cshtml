@model IssueTracker.MVC.Models.Project

<div class="card">
    <div class="card-header">
        <h2>
            Manage Project Users for: @Html.DisplayFor(model => model.ProjectName)
        </h2>
    </div>
    <div class="card-body">         
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <select asp-for="AvaliablePersonnel.Admin.FirstOrDefault().Id"
                        class="form-control"
                        asp-items="@(new SelectList(ViewData.Model.AvaliablePersonnel.Admin, "", ""))">
                    <option>--Select--</option>
                </select>
            </div>
            <div class="form-group">
                <select asp-for="AvaliablePersonnel.Admin.FirstOrDefault().Id"
                        class="form-control"
                        asp-items="@(new SelectList(ViewData.Model.AvaliablePersonnel.Admin, "", ""))">
                    <option>--Select--</option>
                </select>
            </div>
            <div class="form-group">
                <select asp-for="AvaliablePersonnel.Developer.FirstOrDefault().Id"
                        class="form-control" multiple="multiple"
                        asp-items="@(new SelectList(ViewData.Model.AvaliablePersonnel.Developer, "", ""))">                    
                </select>

            </div>
            <div class="form-group">
                <select asp-for="AvaliablePersonnel.ProjectManager.FirstOrDefault().Id"
                        class="form-control" multiple="multiple"
                        asp-items="@(new SelectList(ViewData.Model.AvaliablePersonnel.ProjectManager, "", ""))"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
                
    </div>
</div>

