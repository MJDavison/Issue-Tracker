@model IEnumerable<IssueTracker.MVC.ViewModels.ProjectViewModel>

@{
    ViewData["Title"] = "Index";
}

<p>
    <a asp-action="Create">Create New Project</a>
</p>

<div class="card">
    <div class="card-header">
        <h4>Your Projects</h4>
        <p>All the projects you have in the database</p>
    </div>
    <div class="card-body">
        <table class="table">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Project.Name)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Project.Description)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Project.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Project.Description)
                        </td>
                        <td>
                            <ul>
                                <li>
                                    <a asp-controller="Ticket" asp-action="Create" asp-route-project-id="@item.Project.Id">Create Issue</a>
                                </li>
                                <li>
                                    <a asp-action="ManageUsers" asp-route-id="@item.Project.Id">Manage Users</a>
                                </li>
                                <li>
                                    <a asp-action="Details" asp-route-id="@item.Project.Id">Details</a>
                                </li>
                            </ul>


                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>


